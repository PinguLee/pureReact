범위
var 변수는 전역 범위 또는 함수 범위에서 선언됩니다
let과 const 변수는 블록 범위에서 선언됩니다.

재선언
var 변수는 함수 내에서 재선언이 가능합니다.
let과 const 변수는 재선언이 불가능합니다.

재할당
var 변수와 let 변수는 재할당이 가능합니다.
const 변수는 재할당이 불가능합니다.

호이스팅
var 변수는 호이스팅이 됩니다.
let과 const 변수는 호이스팅이 되지 않습니다.