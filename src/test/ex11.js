// VS Code에서 JSDoc을 사용하여 코드의 API 문서를 생성하면 코드의 이해와 공유가 쉬워집니다.

/**
 * 더하기 함수
 *
 * @param {number} x 첫 번째 매개변수
 * @param {number} y 두 번째 매개변수
 * @returns {number} 두 매개변수의 합
 */

function add(x, y) {
  return x + y;
}